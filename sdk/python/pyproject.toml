[project]
name = "coda"
version = "0.1.0"
description = "OpenCoda Python SDK"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "pyyaml>=6.0",
]

[project.scripts]
coda = "coda.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
